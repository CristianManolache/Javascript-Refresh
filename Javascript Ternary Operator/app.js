var first_name = "Alex";
var last_name = "Bobby";
var age = 26;
var first = true;
var last = false;
var bob;

if (first == last) {
    console.log("This can never happen.");

} else {
    console.log("You're only last if you're not first.");
}

//Ternary statment simple if statment with one simple question can you use the ternary statment
// ()? : ;

first == last ? console.log("nope") : console.log("yep");

first == last ? console.log("no it's not true") : (age) ? console.log("Yes he is 26 'true' ") : console.log("not available");
