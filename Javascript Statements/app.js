var first_name = "Laurentiu";
var last_name = "Manolache";
var age = 26;
var first = true;
var last = false;

//Falsey
// false, null, undefined, 0, "", '', NaN (not an number) ---> Falsey

// "" and '' are considered string but also Falsey


if (age <= 40) {
    //if the value of age is less than or equal to 40
} else if ((age > 40) && (age < 50)) {
    //if age is greater than 40
    // && - means AND
    // || - means OR 
} else if (age < 0) {
    //otherwise prevent something from happening
} else {
    //greater than 50
}

//COMPARISON operators
// < <= > >= == != ! === --> comparing 2 object